// import all the components
import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

// create root
const el = document.getElementById('root');
const root = ReactDOM.createRoot(el);

// render component into the root
root.render(<App/>);